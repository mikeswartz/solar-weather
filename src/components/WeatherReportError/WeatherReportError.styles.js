import { makeStyles } from "@material-ui/core/styles";

const WeatherReportErrorStyles = makeStyles((theme) => ({
  // Insert styles here
  Wrapper: {
    color: "red",
  },
}));

export default WeatherReportErrorStyles;
